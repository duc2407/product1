<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Product</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
    <!-- Trong file EJS -->
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>

</head>
<body>
    <nav class="navbar navbar-default">
        <div class="container-fluid">
          <div class="navbar-header">
            <a class="navbar-brand" href="#">Quản Lý</a>
          </div>
          <ul class="nav navbar-nav">
            <li ><a href="/user-page">Doanh thu</a></li>
            <li class="active"><a href="/product">Danh sách sản phẩm</a></li>
          </ul>
        </div>
    </nav>
    <table class="table table-bordered">
        <thead>
          <tr>
            <th>ID PRODUCT</th>
            <th>Name</th>
            <th>Price</th>
            <th>Quantily</th>
            <th>Type_product</th>
            <th>Sale</th>
          </tr>
        </thead>
        <tbody>
            <tr>
                <form id="createForm" onsubmit="submitFormCreate(event)">
                    <td><input id="id_product"t name="id_product" style="border: none" type="text"/></td>
                    <td><input id="name" name="name" style="border: none" type="text"></td>
                    <td><input id="price" name="price" style="border: none" type="text"></td>
                    <td><input id="quanlity" name="quanlity" style="border: none" type="text"></td>
                    <td><input id="type_product" name="type_product" style="border: none" type="text"></td>
                    <td><input id="sale" name="sale" style="border: none" type="text"></td>
                    <button class="btn btn-primary" type="submit">Thêm</button>
                </form>
            </tr> 
        </tbody>

        <a href="/product" class="btn btn-primary">Quay Lại</a>

      </table>
      <script>
        function submitFormCreate(event) {
        event.preventDefault();

        var formData = {
            id_product: document.getElementById('id_product').value,
            name: document.getElementById('name').value,
            price: document.getElementById('price').value,
            quantily: document.getElementById('quanlity').value,
            type_product: document.getElementById('type_product').value,
            sale: document.getElementById('sale').value
        };

        axios.post('/product/create-product', formData)
            .then(function (response) {
                console.log(formData)
                alert('OK');

                // Xử lý kết quả từ server (nếu cần)
            })
            .catch(function (error) {
                console.error(error);
            });
    }
      </script>
</body>
</html>